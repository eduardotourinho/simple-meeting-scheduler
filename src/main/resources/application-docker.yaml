spring:
  application:
    name: meeting-scheduler

  jpa:
    open-in-view: off

  datasource:
    url: jdbc:postgresql://postgres:5432/scheduler
    username: dev
    password: S3cret!
    driver-class-name: org.postgresql.Driver

  data:
    redis:
      host: redis

  docker:
    compose:
      enabled: false

management:
  tracing:
    sampling:
      probability: 1.0

  otlp:
    metrics:
      export:
        url: http://grafana:4318/v1/metrics

  opentelemetry:
    tracing:
      export:
        otlp:
          endpoint: http://grafana:4318/v1/traces
        include-unsampled: true

    logging:
      export:
        otlp:
          endpoint: http://grafana:4318/v1/logs

springdoc:
  api-docs:
    enabled: true
    path: /api-docs

scalar:
  enabled: true
